# ===== CONFIGURACIÓN DE SEGURIDAD =====
# Copiar este archivo a .env y llenar los valores reales
# NUNCA subir .env a GitHub

# Contraseña del panel administrador (CAMBIAR EN PRODUCCIÓN)
ADMIN_PASSWORD=cifustyle2026

# Clave secreta para firmar tokens JWT (generar una aleatoria en producción)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_jwt_secret_super_largo_aqui_cambiar_en_produccion

# Puerto del servidor
PORT=3000

# Entorno (development o production)
NODE_ENV=development
